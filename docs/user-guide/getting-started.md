# Getting Started with ForecastLab

> **Moved.** This guide has been absorbed into the [user manual](../manual/README.md).

Its content now lives in two chapters:

- **[Installation](../manual/operator/installation.md)** — prerequisites, `.env`, Docker, `uv sync`, migrations, and the pass condition for each step.
- **[Quickstart](../manual/operator/quickstart.md)** — the fastest path to a working system with data and trained models.

New readers should start at the [manual index](../manual/README.md), which splits the documentation into an operator, analyst, and integrator track.
